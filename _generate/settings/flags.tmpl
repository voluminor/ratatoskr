// Code generated using '_generate/settings'; DO NOT EDIT.
// Generation time: {{.GenerationTime}}

package settings

import (
{{- range .FlagsImports }}
    "{{ . }}"
{{- end }}
)

// // // // // // // // // //

{{- if .HasCustomFlags }}

    // CustomFlagTargetsObj holds raw string values for flags that need custom parsing.
    type CustomFlagTargetsObj struct {
    {{- range .Flags }}
        {{- if .IsEnum }}
            {{ flatName .FlagAccessor }} string
        {{- else if .IsArray }}
            {{ flatName .FlagAccessor }} string
        {{- else if isMapType .Type }}
            {{ flatName .FlagAccessor }} string
        {{- else if not (nativeFlag .Type) }}
            {{ flatName .FlagAccessor }} string
        {{- end }}
    {{- end }}
    }
{{ end }}

{{- /* ---- DefineFlags ---- */ -}}

// DefineFlags registers all settings as CLI flags on the given FlagSet.
{{- if .HasCustomFlags }}
    func DefineFlags(fs *flag.FlagSet, obj *Obj, targets *CustomFlagTargetsObj) {
{{- else }}
    func DefineFlags(fs *flag.FlagSet, obj *Obj) {
{{- end }}
{{- range .Flags }}
    {{- if .IsEnum }}
        fs.StringVar(&targets.{{ flatName .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}.String(), "{{ .Usage }}")
    {{- else if .IsArray }}
        fs.StringVar(&targets.{{ flatName .FlagAccessor }}, "{{ .Name }}", "", "{{ .Usage }}")
    {{- else if isMapType .Type }}
        fs.StringVar(&targets.{{ flatName .FlagAccessor }}, "{{ .Name }}", "", "{{ .Usage }}")
    {{- else if eq .Type "string" }}
        fs.StringVar(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else if eq .Type "bool" }}
        fs.BoolVar(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else if eq .Type "int" }}
        fs.IntVar(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else if eq .Type "int64" }}
        fs.Int64Var(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else if eq .Type "uint" }}
        fs.UintVar(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else if eq .Type "uint64" }}
        fs.Uint64Var(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else if eq .Type "float64" }}
        fs.Float64Var(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else if eq .Type "duration" }}
        fs.DurationVar(&obj.{{ .FlagAccessor }}, "{{ .Name }}", obj.{{ .FlagAccessor }}, "{{ .Usage }}")
    {{- else }}
        fs.StringVar(&targets.{{ flatName .FlagAccessor }}, "{{ .Name }}", fmt.Sprint(obj.{{ .FlagAccessor }}), "{{ .Usage }}")
    {{- end }}
{{- end }}
}

{{- if .HasCustomFlags }}

    // //

    // ApplyCustomFlags converts raw string flag values into typed struct fields.
    // Only applies values for flags that were explicitly set via CLI.
    func ApplyCustomFlags(fs *flag.FlagSet, targets *CustomFlagTargetsObj, obj *Obj) error {
    var err error
    _ = err

    fs.Visit(func(f *flag.Flag) {
    if err != nil {
    return
    }
    switch f.Name {
    {{- range .Flags }}
        {{- if .IsEnum }}
            {{- if .IsArray }}
                case "{{ .Name }}":
                parts := strings.Split(targets.{{ flatName .FlagAccessor }}, ",")
                obj.{{ .FlagAccessor }} = make({{ .GoType }}, 0, len(parts))
                for _, p := range parts {
                p = strings.TrimSpace(p)
                if p == "" {
                continue
                }
                var v {{ .EnumType }}
                v, err = Parse{{ .EnumType }}(p)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, v)
                }
            {{- else }}
                case "{{ .Name }}":
                var v {{ .EnumType }}
                v, err = Parse{{ .EnumType }}(targets.{{ flatName .FlagAccessor }})
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = v
            {{- end }}
        {{- else if .IsArray }}
            case "{{ .Name }}":
            parts := strings.Split(targets.{{ flatName .FlagAccessor }}, ",")
            obj.{{ .FlagAccessor }} = make({{ .GoType }}, 0, len(parts))
            for _, p := range parts {
            p = strings.TrimSpace(p)
            if p == "" {
            continue
            }
            {{- if eq (baseArrayType .Type) "string" }}
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, p)
            {{- else if eq (baseArrayType .Type) "int" }}
                var iv int
                iv, err = strconv.Atoi(p)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, iv)
            {{- else if eq (baseArrayType .Type) "int64" }}
                var iv int64
                iv, err = strconv.ParseInt(p, 10, 64)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, iv)
            {{- else if eq (baseArrayType .Type) "bool" }}
                var bv bool
                bv, err = strconv.ParseBool(p)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, bv)
            {{- else if eq (baseArrayType .Type) "float32" }}
                var fv float64
                fv, err = strconv.ParseFloat(p, 32)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, float32(fv))
            {{- else if eq (baseArrayType .Type) "float64" }}
                var fv float64
                fv, err = strconv.ParseFloat(p, 64)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, fv)
            {{- else if eq (baseArrayType .Type) "uint" }}
                var uv uint64
                uv, err = strconv.ParseUint(p, 10, 64)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, uint(uv))
            {{- else if eq (baseArrayType .Type) "uint64" }}
                var uv uint64
                uv, err = strconv.ParseUint(p, 10, 64)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, uv)
            {{- else if eq (baseArrayType .Type) "duration" }}
                var dv time.Duration
                dv, err = time.ParseDuration(p)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = append(obj.{{ .FlagAccessor }}, dv)
            {{- end }}
            }
        {{- else if isMapType .Type }}
            case "{{ .Name }}":
            if s := targets.{{ flatName .FlagAccessor }}; s != "" {
            err = json.Unmarshal([]byte(s), &obj.{{ .FlagAccessor }})
            if err != nil {
            return
            }
            }
        {{- else if not (nativeFlag .Type) }}
            {{- if eq .Type "int8" }}
                case "{{ .Name }}":
                var iv int64
                iv, err = strconv.ParseInt(targets.{{ flatName .FlagAccessor }}, 10, 8)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = int8(iv)
            {{- else if eq .Type "int16" }}
                case "{{ .Name }}":
                var iv int64
                iv, err = strconv.ParseInt(targets.{{ flatName .FlagAccessor }}, 10, 16)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = int16(iv)
            {{- else if eq .Type "int32" }}
                case "{{ .Name }}":
                var iv int64
                iv, err = strconv.ParseInt(targets.{{ flatName .FlagAccessor }}, 10, 32)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = int32(iv)
            {{- else if eq .Type "uint8" }}
                case "{{ .Name }}":
                var uv uint64
                uv, err = strconv.ParseUint(targets.{{ flatName .FlagAccessor }}, 10, 8)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = uint8(uv)
            {{- else if eq .Type "uint16" }}
                case "{{ .Name }}":
                var uv uint64
                uv, err = strconv.ParseUint(targets.{{ flatName .FlagAccessor }}, 10, 16)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = uint16(uv)
            {{- else if eq .Type "uint32" }}
                case "{{ .Name }}":
                var uv uint64
                uv, err = strconv.ParseUint(targets.{{ flatName .FlagAccessor }}, 10, 32)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = uint32(uv)
            {{- else if eq .Type "float32" }}
                case "{{ .Name }}":
                var fv float64
                fv, err = strconv.ParseFloat(targets.{{ flatName .FlagAccessor }}, 32)
                if err != nil {
                return
                }
                obj.{{ .FlagAccessor }} = float32(fv)
            {{- end }}
        {{- end }}
    {{- end }}
    }
    })

    return err
    }
{{ end }}
